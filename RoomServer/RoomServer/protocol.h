#pragma once
enum SrcDstType
{
	MATCHING_SERVER = 0,
	MATCHING_CLIENT,
	ROOM_SERVER,
	PACKET_GENERATOR,
	MONITORING_SERVER,
	CONFIG_SERVER,
	CONNECTION_SERVER
};

enum Command
{
	MATCH_REQUEST = 0,
	MATCH_RESPONSE,
	LATENCY,
	HEALTH_CHECK,
	MSLIST_REQUEST,
	MSLIST_RESPONSE,
	PG_START,
	PG_END,
	ROOM_CREATE_REQUEST,
	ROOM_CREATE_RESPONSE,
	ROOM_JOIN_REQUEST,
	ROOM_JOIN_RESPONSE,
	GAME_START,
	GAME_END,
};

enum Status
{
	SUCCESS = 0,
	FAIL,
	NONE
};


struct Body
{
	Command command;
	Status status;
	char* data;
};
